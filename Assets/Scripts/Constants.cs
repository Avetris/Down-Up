using UnityEngine;
using System.Collections;

public class Constants
{
    public enum GameStatus { STOPPED, TUTORIAL, PLAYING }
    public enum ObstacleType { CLOUD_1, CLOUD_2, EAGLE, ASTEROID, SPACESHIP, SPIKETRAP, /* ROCKET, BALOOM, VERTICAL_WALL */ };
    public enum ObstaclesMovement { ONE_DIMENSION, TWO_DIMENSIONS, STATIC}

}
