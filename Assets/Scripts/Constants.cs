using UnityEngine;
using System.Collections;

public class Constants
{
    public enum GameStatus { STOPPED, TUTORIAL, PLAYING }
    public enum ObstacleType { /*CLOUD_1, CLOUD_2, EAGLE, */ASTEROID };
    public enum ObstaclesMovement { LEFT_RIGHT, DIAGONAL, ZIG_ZAG}

}
